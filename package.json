{
  "name": "once.ts",
  "namespace": "tla.EAM",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "dist/2_systems/Once.class.js",
  "scripts": {
    "prepare": "npm-run-all prepare:submodules prepare:ts-patch prepare:build prepare:install prepare:cleanup",
    "prepare:submodules": "git submodule update --init --recursive",
    "prepare:ts-patch": "ts-patch install",
    "prepare:build": "tsc --project tsconfig.install.json",
    "prepare:install": "cross-env NODE_ENV=install_once node .",
    "prepare:cleanup": "rm -rf dist",
    "build:version":"tsc -p tsconfig.build.json",
    "start": "node src/2_systems/Once.class.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ONCE-DAO/once.ts.git"
  },
  "author": "",
  "license": "AGPL-3.0",
  "bugs": {
    "url": "https://github.com/ONCE-DAO/once.ts/issues"
  },
  "homepage": "https://github.com/ONCE-DAO/once.ts#readme",
  "devDependencies": {
    "@types/chmodr": "^1.0.0",
    "@types/node": "^17.0.13",
    "nodemon": "^2.0.15",
    "npm-run-all": "^4.1.5",
    "ts-node": "^10.4.0",
    "ts-patch": "^2.0.1",
    "typescript": "^4.5.5"
  },
  "dependencies": {
    "chmodr": "^1.2.0",
    "cross-env": "^7.0.3",
    "dotenv": "^14.3.0",
    "os": "^0.1.2",
    "simple-git": "^3.1.0"
  }
}
